language: python
matrix:
  include:
    - python: 3.6
    - python: 3.7
      sudo: true
      dist: xenial
install:
  - pip install . coveralls
script:
  - coverage run --source="." --omit="*/test_*","env/*","setup.py" -m unittest discover -s src -v
after_success:
  - coveralls
